# ABOUTME: Line endings for the files whose format depends on them. A shell hook checked out with
# CRLF fails on its own shebang, so the hooks are pinned to LF wherever they are cloned.

.githooks/* text eol=lf
