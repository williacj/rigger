ABOUTME: Records why card 203 made the owner's merge the owner's ratification and verdict, what
the sweep for sentences on how the owner rules found, and what it left for M5.

# 2026-09-24 — One act, read as two

Both registers' preambles had the owner ratify by returning a sound verdict, with the entry
binding once the pull request merged. The owner performs one act, the merge. So a coordinator
reading the preambles waited for a verdict nobody would post, flagged `D20` as merged without
one, and relayed a needs revision onto #196 after the merge had already ratified it.

The fix is in the preambles, and `D21` holds the reasoning. The consequence that mattered more
was `D19`'s: a maker merges once every configured judge's verdict is in hand. Where the owner is a
judge and the merge is the owner's verdict, that evidence is complete only after the merge, so
the maker of an owner-judged card never merges it. `AGENTS.md`'s "Review and merge" now says so.

The sweep for sentences on how the owner ratifies or rules was a stem grep over the binding
documents and the prompts and skills, 498 lines at `a440446`. Two sentences outside the
preambles carried the two-event reading without the word "verdict". The architect prompt said
the register holds a choice "after owner ratification and merge". The PM prompt said "your
judges rule on the proposal before it lands", where the owner's ruling is the landing.
`AGENTS.md`'s judge paragraph had every judge record each item met or unmet in a marker, which
the owner's merge does not do. Each now names the agent judges, or the merge, instead.

The product side was left alone on purpose. `README.md`, `R-GATE-5`, `R-LOOP-11` and M5 in the
build plan describe a deployment where the owner's verdict is a marker the gate reads before it
admits a merge. That is what Rigger promises a consumer, and this card changes how this
repository works before its own gate exists. The two meet at M5, when the hook arrives here and
refuses a merge lacking any configured judge's verdict.

Round 1 first wrote `D21` as permanent, with M5 as a possible reversal. Two judges found that it
mixed a lifespan ending at M5 with one that had no end, which the register's preamble forbids. The
owner ruled that the whole arrangement runs until M5, on `D19`'s lifespan. So `D21`, both
preambles and the `AGENTS.md` paragraph now say "until M5". `D21`'s deferred table holds the four
questions M5 must settle, and settles none of them.

Round 1's census also missed sentences where the owner is "you" or goes unnamed, because it
filtered the product documents by the words "owner" and "ratify". `README.md` calls the owner
"you" throughout. Round 2 read those documents by meaning instead.

One thing nothing observes: every session here acts through the owner's forge account, so the
forge cannot tell the owner's merge from a session's. The rule holds because sessions honour it.
