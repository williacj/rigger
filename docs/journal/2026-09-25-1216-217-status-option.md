ABOUTME: Records card #217, which gives the forge adapter `createColumn`, the write adding a
`Status` option, by the route #212's spike found keeps every item's column.

# 2026-09-25 — A column, added the way the spike found safe

#212's report names one route that adds a `Status` option and keeps every item's column. It is
way B: `updateProjectV2Field` sending every held option with its `id`, name, colour and
description, and the new option with no `id`. The schema-write side sends exactly that, and the
schema-write runner admits nothing else under `updateProjectV2Field`.

The runner does not trust the side's copy of the options. It reads the field's options itself,
through the read runner, straight before the write. The item-write runner does the same with the
field holding the columns. A held option that is missing, sent without its `id`, or sent with a
name, colour or description other than its own is refused, and so is a new option carrying an
`id` the field does not hold. Way C, every option without its `id`, is refused by the first rule.
The request is named in every refusal, by operation, field and option.

The option read's answer shape comes from gh 2.99.0 on 2026-09-25, recorded against board 6's
`Status` field. Only reads were made. Board 6 holds no `Owner` option yet, and adding one is
#235's work.

The fake `gh` models way B alone. An options write that leaves out a held option or its `id`
fails as unmodelled, because on GitHub it cleared every column, and the fake board has no way to
hold that. `gh-recording.mjs` now records the schema-write runner's own read the same way it
records the item-write runner's. Without that, the set-equality test found the fake answering a
command the adapter never seemed to issue.

With the probe `src/scheduling/c217-probe.mjs` importing `schemaWriteSide`, #213's boundary test
failed rule 1, and rule 6 as well, with no edit to the test. The probe was deleted before commit.

A mutation that disabled the check for a left-out option redded the way C test and the left-out
test on their naming assertions. The file ran 31 tests, its usual count. The mutation was reverted
before commit.

Round 1's Codex judge ruled item 4 unmet. I had checked every held option exactly, but checked a
new option only for having no `id`. So `{}` as the new option reached `gh`, and so did a write that
added no option at all. Neither is way B's shape. Now every option has to carry a name,
colour and description of the kinds way B writes, and at least one option has to be new. The fake
`gh` had the same gap. It checked only held options' ids, so a write that recoloured a held option
was answered as a plain add. It now fails as unmodelled.

On the mutant, the left-out option does not reach `gh` even with the check gone. The next loop
dereferences the missing entry and throws a `TypeError`, so the request still fails closed. It
just names nothing. The two tests red on their naming assertion, and that is the right reason.
