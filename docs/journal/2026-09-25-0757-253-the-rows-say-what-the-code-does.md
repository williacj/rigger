ABOUTME: Records card #253, the PM's proposal bringing R-CARD-12's rows and the R-SCHED group into
line with the owner's rulings of 2026-09-25: which rows kept their ids, and where the code outran a ruling.

# 2026-09-25 — The rows say what the code does

The card asked the register to state rulings #252 had already built. The id test in the
requirements preamble decided the shape. It asks whether an edit changes what must be true of
Rigger. Ruling (e) did: `R-CARD-16` said a closer "holds only a run", so a closer with trailing
spaces left the fence open. That row is withdrawn and `R-CARD-26` replaces it. The exclusions for
comment-only items and thematic breaks went into new rows beside `R-CARD-25`, rather than into
it, so no ratified row's truth moved under its own id.

`R-CARD-15` kept its id, and that is a judgment the owner should see. Read literally, the ratified
row let a tilde line inside a backtick fence open a second block. No ratified row said what two
open blocks do, and #218's definition, which the rows claim to state, reads one block at a time.

Stating the code exactly turned up one place where it is wider than the ruling. Ruling (c) says a
bullet "holding only HTML comments". The code reads any text that opens with `<!--` and ends with
`-->`, so `- <!-- a --> real <!-- b -->` is no item. The row says what the code does, because the
card fails a row that differs from #252's code. The pull request puts the gap to the owner.

A `spec` card wrote four test bodies here, because two rows' clauses had nothing pinning them: the
nested fence opener and the item that opens a comment a later line closes. The other two pin
the no-break space and the order of deletion and collapse. Each passed on arrival, since the code
already behaved so; the mutations are what show each one discriminates.
