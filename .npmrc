# ABOUTME: npm settings for this repository. engine-strict turns the Node floor in package.json
# from a warning into a refusal, so an install under an older Node fails and names it.
engine-strict=true
